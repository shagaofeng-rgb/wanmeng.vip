import {notFound} from 'next/navigation';import Link from 'next/link';import {getProduct,products} from '@/data/products';import {Gallery} from '@/components/Interactive';import {ProductCard} from '@/components/Site';
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getProduct(slug);if(!p)notFound();return <><section className="product-detail section"><div><p className="crumb"><Link href="/">首页</Link> / <Link href="/products">产品中心</Link> / {p.name}</p><Gallery images={p.images} name={p.name}/></div><div className="product-info"><p className="eyebrow">{p.category}</p><h1>{p.name}</h1><p className="lead">给晚睡室友的一点理解，也给自己一晚不被灯光打扰的睡眠。</p><div className="tags">{p.tags.map(t=><span key={t}>{t}</span>)}</div><p>采用柔和、耐用的面料与易安装设计，为宿舍生活留出一块更安静的私人空间。</p><dl><div><dt>适用床位</dt><dd>上铺 / 下铺</dd></div><div><dt>颜色</dt><dd>{p.color}</dd></div><div><dt>安装方式</dt><dd>挂钩 / 支架适配</dd></div><div><dt>包装内容</dt><dd>床帘主体、挂钩、安装说明</dd></div></dl><Link className="btn" href="/inquiry">获取尺寸建议</Link><Link className="text-link" href="/contact">咨询购买渠道 →</Link></div></section><section className="soft section"><div className="detail-points">{[['挡住光，不挡住空气','在遮光与日常通风之间，留出舒服的平衡。'],['保护隐私，也保留舒适感','拉合顺手，换衣、休息时更从容。'],['日常使用，依然轻松打理','建议低温轻柔清洗，收纳前保持干燥。']].map(x=><article key={x[0]}><h3>{x[0]}</h3><p>{x[1]}</p></article>)}</div></section><section className="section"><h2>可能也适合你</h2><div className="product-grid">{products.filter(x=>x.slug!==p.slug).slice(0,3).map(x=><ProductCard key={x.slug} p={x}/>)}</div></section></>}
